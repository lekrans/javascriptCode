const log = require('lekrans_code_helper');

const appName = 'length';
log.header(appName);

log.subHeader1('Show the length of an array');

log.comment('[1, 2, 3, 4, 5, 6].length');
log.log([1, 2, 3, 4, 5, 6].length);

log.footer(appName);
